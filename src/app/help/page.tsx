import ThreePartInput from '@/components/ThreePartInput';

export default function Help() {
    return (
        <main className="w-full min-h-screen flex items-center justify-center">
            <ThreePartInput />
        </main>
    );
}