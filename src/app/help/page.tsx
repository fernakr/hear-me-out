import PredictionInput from '@/components/PredictionInput';

export default function Help() {
    return (
        <main className="w-full min-h-screen flex items-center justify-center py-5 px-8">
            <PredictionInput />
        </main>
    );
}